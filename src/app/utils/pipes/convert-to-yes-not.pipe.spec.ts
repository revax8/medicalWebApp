import { ConvertToYesNotPipe } from './convert-to-yes-not.pipe';

describe('ConvertToYesNotPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToYesNotPipe();
    expect(pipe).toBeTruthy();
  });
});
